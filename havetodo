
//	segfault	v ./cub3D good_cub/cheese_maze.cub
// psque ya .cub mais dcp ca va dans wgas et c`est cense exit

# minimap cub

- prendre la pos du joueur x, y par rapport à la matrice
- afficher un carré de la map avec le point du joueur au milieu :
    - s’il est dans un coin bloquer pour ne pas afficher du vide à l’emplacement de la map
    - ajouter un effet transparent avec alpha RGB
    - afficher un quadrillage → si on est sur la premiere ligne ou bien premiere colonne le pixel est noir


creer un buffer [height][width] = moitie ceiling/floor

    
    
    Textures :	afficher les bonnes textures en fonction des murs
    
    
    souris //
	pour regarder en haut ou en bas :
	
	simplement faire que ray->startp commence plus tot/tard
	ray->endp commence plus tot/tard
    



	SEGFAULT ERA
    
    segfault avec map at.cub psque ya des tab dans la map et un tab est un character seulement
    
	    {
		    // printf("longueur : %d et Largeur : %d\n", cub->map->l, cub->map->L);
		    // for (int y = 0; cub->map->matrix[y]; ++y) {
		    // 	for (int x = 0; cub->map->matrix[y][x]; ++x) {
		    // 		printf("%c", cub->map->matrix[y][x]);
		    // 		if (!cub->map->matrix[y][x + 1])
		    // 			printf("\nl de cette line = %d\n", x);
		    // 	}
		    // 	printf("\n");
    
		    // }
		    // if (cub->map->matrix[34][3] == '1') {
		    // 	printf("on rentre dans la condition\n");
		    // 	freend(cub);
		    // }
		    // freend(cub);
		    // exit (0);
	    }